# media/

Curated visual artifacts.

## Layout

```
media/
├── screenshots/
│   ├── architecture.png             5-stage pipeline overview
│   ├── architecture-detailed.png    Same architecture, expanded with all tools
│   ├── bakeoff-*.png                6-model side-by-side query screenshots
│   └── lift-demo-*.png              Pure vector vs hybrid filter side-by-side
└── video/
    ├── demo.mp4                     7-frame walkthrough of the system
    └── demo.gif                     Same content, looping GIF
```

The architecture images are referenced inline from the top-level `README.md`. The bake-off and lift-demo screenshots are the receipts behind the two findings in the README.
